Base bordered container for HUD/editor chrome — unit inspector, layer list, minimap frame. Every floating panel in Atlas is built on this.

```jsx
<Panel title="Unit Inspector" actions={<IconButton icon={<CloseIcon/>} size="sm" />}>
  ...content...
</Panel>
```
