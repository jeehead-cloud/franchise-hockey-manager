Underline-style tab switcher for panels (Inspector / Layers / History).

```jsx
<Tabs items={[{value:"props",label:"Properties"},{value:"layers",label:"Layers"}]} value={tab} onChange={setTab} />
```
