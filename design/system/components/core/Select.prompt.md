Native-backed dropdown for short option lists (layer type, unit filter, difficulty).

```jsx
<Select value={brush} onChange={e => setBrush(e.target.value)} options={[{value:"forest",label:"Forest"},{value:"water",label:"Water"}]} />
```
